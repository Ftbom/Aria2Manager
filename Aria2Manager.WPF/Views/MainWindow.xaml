<mah:MetroWindow x:Class="Aria2Manager.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:loc="clr-namespace:Aria2Manager.Core.Localization;assembly=Aria2Manager.Core"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        TitleBarHeight="25"
        TitleForeground="{DynamicResource MahApps.Brushes.ThemeForeground}"
        WindowTitleBrush="{DynamicResource MahApps.Brushes.Accent3}"
        ShowIconOnTitleBar="False"
        Closing="MainWindow_Closing"
        Title="Aria2Manager" Height="480" Width="900" Icon="/Resources/app.ico" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top" MinHeight="30">
            <MenuItem Header="{x:Static loc:Strings.Edit}">
                <MenuItem Header="{x:Static loc:Strings.Add}" Command="{Binding OpenAria2NewTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="+" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Pause}" Command="{Binding PauseTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF04C;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Resume}" Command="{Binding UnpauseTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF04B;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Remove}" Command="{Binding RemoveTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF1F8;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Properties}" Command="{Binding OpenAria2TaskInfoCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xf15c;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{x:Static loc:Strings.Resume_All}" Command="{Binding UnpauseAllTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF144;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Pause_All}" Command="{Binding PauseAllTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF28B;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Purge_All}" Command="{Binding PurgeAllTaskCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xf12d;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="{x:Static loc:Strings.Tools}">
                <MenuItem Header="{x:Static loc:Strings.Aria2_Servers}" Command="{Binding OpenAria2ServersCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF828;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Aria2_Options}" Command="{Binding OpenAria2OptionsCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF1DE;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Program_Settings}" Command="{Binding OpenSettingsCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF013;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{x:Static loc:Strings.Exit}" Command="{Binding ExitCommand}" >
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF00D;" FontFamily="{StaticResource FontAwesome}" FontSize="20" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="{x:Static loc:Strings.Help}" InputGestureText="">
                <MenuItem Header="{x:Static loc:Strings.Aria2_Website}" Command="{Binding OpenAria2WebsiteCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF0AC;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="{x:Static loc:Strings.Aria2_Status}" Command="{Binding OpenAria2StatusCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF059;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{x:Static loc:Strings.About}" Command="{Binding OpenAboutCommand}">
                    <MenuItem.Icon>
                        <TextBlock Text="&#xF05A;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top" MinHeight="37">
            <ToolBar>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding OpenAria2NewTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="+" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Add}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding RemoveTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF1F8;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Remove}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding UnpauseTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF04B;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Resume}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding PauseTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF04C;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Pause}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0"  Command="{Binding OpenAria2TaskInfoCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xf15c;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Properties}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Separator/>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding UnpauseAllTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF144;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Resume_All}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding PauseAllTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF28B;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Pause_All}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,5,0" Command="{Binding PurgeAllTaskCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xf12d;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontSize="24" Margin="0,0,2,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Purge_All}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>
        <StatusBar DockPanel.Dock="Bottom" Background="{DynamicResource MahApps.Brushes.Gray10}">
            <StatusBarItem Height="37" VerticalAlignment="Center" HorizontalAlignment="Left">
                <StackPanel Orientation="Horizontal">
                    <ComboBox ItemsSource="{Binding ServerNames, Mode=OneWay}" SelectedItem="{Binding CurrentServerName}" Margin="2,0,2,0"/>
                    <Ellipse Width="16" Height="16">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Border.NonActive}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ServerStatus.IsConnected, Mode=OneWay}" Value="True">
                                        <Setter Property="Fill" Value="{DynamicResource MahApps.Brushes.Accent}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem Height="37" VerticalAlignment="Center" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Text="{x:Static loc:Strings.Upload_Speed}" Margin="0,0,2,0"/>
                    <TextBlock Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" TextWrapping="Wrap" Text="{Binding ServerUploadSpeed, Mode=OneWay}" Margin="0,0,2,0"/>
                    <TextBlock Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" TextWrapping="Wrap" Text="," Margin="0,0,2,0"/>
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" Text="{x:Static loc:Strings.Download_Speed}" Margin="0,0,2,0"/>
                    <TextBlock Foreground="{DynamicResource MahApps.Brushes.ThemeForeground}" TextWrapping="Wrap" Text="{Binding ServerDownloadSpeed, Mode=OneWay}" Margin="0,0,2,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="100*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>
            <ListView Grid.Column="2" ItemsSource="{Binding Aria2TaskCollection}" SelectedValue="{Binding SelectedTaskGid}" SelectedValuePath="Gid">
                <ListView.ContextMenu >
                    <ContextMenu>
                        <MenuItem Header="{x:Static loc:Strings.Pause}" Command="{Binding PauseTaskCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF04C;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="{x:Static loc:Strings.Resume}" Command="{Binding UnpauseTaskCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF04B;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="{x:Static loc:Strings.Remove}" Command="{Binding RemoveTaskCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xF1F8;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="{x:Static loc:Strings.Properties}" Command="{Binding OpenAria2TaskInfoCommand}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xf15c;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="{x:Static loc:Strings.Open_File_Location}" Command="{Binding OpenFileLocationCommand}" Visibility="{Binding ServerStatus.IsLocal, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <MenuItem.Icon>
                                <TextBlock Text="&#xf07c;" FontFamily="{StaticResource FontAwesome}" FontSize="15" />
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </ListView.ContextMenu>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{x:Static loc:Strings.Name}" Width="200" DisplayMemberBinding="{Binding Name, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Progress}" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <ProgressBar Maximum="100" Value="{Binding Progress, Mode=OneWay}" Width="110" Height="15"/>
                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Progress, Mode=OneWay}"/>
                                    </Grid>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{x:Static loc:Strings.Size}" Width="120" DisplayMemberBinding="{Binding Size, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Downloaded}" Width="120" DisplayMemberBinding="{Binding Downloaded, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.ETA}" Width="120" DisplayMemberBinding="{Binding ETA, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Status}" Width="120" DisplayMemberBinding="{Binding Status, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Download_Speed}" Width="120" DisplayMemberBinding="{Binding DownloadSpeed, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Uploaded}" Width="120" DisplayMemberBinding="{Binding Uploaded, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Upload_Speed}" Width="120" DisplayMemberBinding="{Binding UploadSpeed, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Ratio}" Width="120" DisplayMemberBinding="{Binding Ratio, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Connections}" Width="120" DisplayMemberBinding="{Binding Connections, Mode=OneWay}"/>
                        <GridViewColumn Header="{x:Static loc:Strings.Seeds}" Width="120" DisplayMemberBinding="{Binding NumSeeders, Mode=OneWay}"/>
                    </GridView>
                </ListView.View>
            </ListView>
            <ListBox SelectedValuePath="Tag" SelectedValue="{Binding SelectedStatus}">
                <ListBoxItem Tag="all" Margin="5,5,0,5" IsSelected="True">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF111;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent3}" FontSize="20" Margin="0,0,5,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Status_All}" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem Tag="active" Margin="5,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF358;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent3}" FontSize="20" Margin="0,0,5,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Status_Active}" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem Tag="waiting" Margin="5,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF017;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent3}" FontSize="20" Margin="0,0,5,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Status_Waiting}" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListBoxItem>
                <ListBoxItem Tag="stopped" Margin="5,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xF28D;" FontFamily="{StaticResource FontAwesome}" Foreground="{DynamicResource MahApps.Brushes.Accent3}" FontSize="20" Margin="0,0,5,0" />
                        <TextBlock TextWrapping="Wrap" Text="{x:Static loc:Strings.Status_Stopped}" VerticalAlignment="Center"/>
                    </StackPanel>
                </ListBoxItem>
            </ListBox>
        </Grid>
    </DockPanel>
</mah:MetroWindow>
